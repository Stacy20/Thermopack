/** Índices fijos del bloque “cifras” del hero (3 pares valor / etiqueta). */
export const HOME_HERO_SPOTLIGHT_STAT_INDICES = [0, 1, 2] as const

/** Tarjetas inferiores del hero (2 bloques). */
export const HOME_HERO_TILE_INDICES = [0, 1] as const

/** Columnas de la barra blanca inferior (4 pares). */
export const HOME_HERO_STATSBAR_INDICES = [0, 1, 2, 3] as const
