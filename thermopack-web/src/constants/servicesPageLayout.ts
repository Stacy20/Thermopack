/** Degradados de fondo de cada tarjeta de servicio (rotan por índice). */
export const SERVICE_CARD_GRADIENT_SLOT_INDICES = [0, 1, 2, 3, 4, 5] as const

/** Pasos del bloque “¿Cómo trabajamos?”. */
export const SERVICE_PROCESS_STEP_INDICES = [0, 1, 2, 3] as const
